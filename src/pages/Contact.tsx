import Transition from "../transition";

const Contact = () => {
  return (
    <div className="h-screen w-screen dark:text-white flex justify-center items-center">
      <h1 className="text-4xl uppercase">Contact</h1>
    </div>
  );
};

export default Transition(Contact);
